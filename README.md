# DOMS — Dynamic Order Management System

نظام إدارة طلبات ديناميكي مبني على الويب يسمح بتصميم نماذج مخصصة وإدارة الطلبات بدون الحاجة للبرمجة.

## 📋 نظرة عامة

DOMS هو تطبيق ويب تفاعلي يوفر:
- ✨ **تصميم نماذج ديناميكي** - أضف حقول مخصصة بأنواع مختلفة
- 📊 **إدارة طلبات متكاملة** - عرض، بحث، فلترة، وتصدير
- 💾 **تخزين مرن** - محلي أو عبر Supabase السحابي
- 📱 **تصميم متجاوب** - يعمل على جميع الأجهزة
- 🌐 **دعم كامل للغة العربية**

## 🏗️ بنية المشروع

```
مكتبه/
├── index.html              # الصفحة الرئيسية
├── css/
│   └── styles.css          # الأنماط CSS الكاملة
├── js/
│   ├── app.js              # التطبيق الرئيسي
│   ├── config.js           # إعدادات التخزين المحلي
│   ├── engine.js           # المحرك الأساسي للنظام
│   ├── storage.js          # طبقة التخزين (محلي/سحابي)
│   ├── ui-orders.js        # واجهة إدارة الطلبات
│   └── ui-schema.js        # واجهة تصميم النماذج
└── supabase/
    ├── schema.sql          # مخطط قاعدة البيانات
    └── migrate_orders_columns.sql  # ترحيل البيانات
```

## 🚀 المميزات الرئيسية

### 📝 تصميم النماذج
- **حقول ديناميكية**: نص، رقم، بريد إلكتروني، هاتف، تاريخ، اختيار، متعدد الاختيارات، منطقي، رابط، نص طويل
- **خصائص الحقل**: تحديد الإلزامية، الظهور في النموذج/الجدول/البحث/الفلترة
- **ترتيب مخصص**: إعادة ترتيب الحقول بالسحب والإفلات
- **المفتاح التقني (slug)**: اسم فريد لكل حقل يستخدم في قاعدة البيانات

### 📊 إدارة الطلبات
- **عرض جدولي**: عرض جميع الطلبات في جدول منظم
- **بحث شامل**: البحث في جميع الحقول والبيانات
- **فلترة متقدمة**: فلترة حسب الحالة والحقول المخصصة
- **تصدير البيانات**: تصدير إلى CSV و JSON
- **حالات الطلب**: جديد، قيد المعالجة، مكتمل، ملغي

### 💾 التخزين
- **محلي**: استخدام localStorage للتخزين المحلي
- **سحابي**: التكامل مع Supabase لقواعد البيانات السحابية
- **نسخ احتياطي**: تصدير واستيراد البيانات

## 🛠️ التقنيات المستخدمة

### الواجهة الأمامية
- **HTML5** - هيكل الصفحات
- **CSS3** - الأنماط والتصميم المتجاوب
- **JavaScript (ES6+)** - المنطق والتفاعل
- **Web Components** - مكونات قابلة لإعادة الاستخدام

### قاعدة البيانات
- **localStorage** - التخزين المحلي
- **Supabase** - قاعدة البيانات السحابية (PostgreSQL)

### المكتبات الخارجية
- **Google Fonts (Tajawal)** - الخط العربي
- **Supabase JS Client** - الاتصال بقاعدة البيانات

## 📦 التثبيت والتشغيل

### التشغيل المحلي
1. **تحميل المشروع**
   ```bash
   git clone <repository-url>
   cd مكتبه
   ```

2. **تشغيل الخادم المحلي**
   ```bash
   # استخدام Python
   python -m http.server 8000
   
   # أو استخدام Node.js
   npx serve .
   
   # أو استخدام Live Server في VS Code
   ```

3. **فتح التطبيق**
   - افتح المتصفح على `http://localhost:8000`

### الإعداد السحابي مع Supabase

1. **إنشاء مشروع Supabase**
   - اذهب إلى [supabase.com](https://supabase.com)
   - أنشئ مشروعًا جديدًا

2. **إنشاء الجداول**
   - افتح SQL Editor في مشروع Supabase
   - نفّذ محتويات `supabase/schema.sql`

3. **إعداد الاتصال**
   - في التطبيق: اذهب إلى الإعدادات
   - أدخل Project URL و anon public key
   - فعّل "استخدام Supabase"

## 📖 دليل الاستخدام

### 1. تصميم النموذج
1. اذهب إلى "تصميم النموذج" من القائمة الجانبية
2. اضغط "خانة جديدة" لإضافة حقل
3. حدد:
   - **المفتاح الداخلي (slug)**: اسم تقني بالإنجليزية
   - **التسمية**: الاسم المعروض للمستخدم
   - **نوع الحقل**: نص، رقم، تاريخ، إلخ
   - **الخصائص**: إلزامي، ظهور في النموذج/الجدول/البحث

### 2. إدارة الطلبات
1. اذهب إلى "الطلبات" من القائمة الجانبية
2. اضغط على زر الإضافة لإنشاء طلب جديد
3. املأ الحقول واضغط "حفظ الطلب"
4. استخدم شريط البحث والفلترة للعثور على الطلبات

### 3. التصدير والنسخ الاحتياطي
1. **تصدير CSV**: من صفحة الطلبات اضغط "تصدير CSV"
2. **تصدير JSON**: من الإعدادات اضغط "تصدير JSON"
3. **استيراد**: من الإعدادات اختر ملف JSON للاستيراد

## 🗄️ مخطط قاعدة البيانات

### جدول الحقول (schema_fields)
```sql
- id: المعرّف الفريد
- slug: المفتاح التقني (مثل customer_name)
- label: التسمية المعروضة
- field_type: نوع الحقل
- required: هل الحقل إلزامي
- show_in_form: الظهور في نموذج الإدخال
- show_in_table: الظهور في جدول العرض
- searchable: إمكانية البحث فيه
- filterable: إمكانية الفلترة
- sort_order: ترتيب العرض
- options: خيارات إضافية (JSON)
```

### جدول الطلبات (orders)
```sql
- id: المعرّف الفريد
- reference: رمز المرجع الفريد
- status: حالة الطلب
- created_at: تاريخ الإنشاء
- data: البيانات الديناميكية (JSON)
```

## 🎨 التخصيص

### إضافة أنواع حقول جديدة
1. عدّل `js/engine.js` - أضف النوع الجديد إلى `FIELD_TYPES`
2. عدّل `js/ui-schema.js` - أضف واجهة الإعداد للنوع الجديد
3. عدّل `css/styles.css` - أضف الأنماط الخاصة بالنوع

### تعديل الألوان والثيم
- عدّل المتغيرات في بداية `css/styles.css`
- المتغيرات الرئيسية: `--primary`, `--secondary`, `--success`

## 🔧 الإعدادات المتقدمة

### متغيرات التكوين
```javascript
// في js/config.js
DOMS.config = {
  SCHEMA_LOCAL: 'doms_schema_v1',     // مفتاح التخزين المحلي للمخطط
  ORDERS_LOCAL: 'doms_orders_v1',     // مفتاح التخزين المحلي للطلبات
}
```

### سياسات الأمان
- النظام يدعم RLS (Row Level Security) في Supabase
- يمكن تقييد الوصول حسب المستخدمين
- يدعم المصادقة عبر Supabase Auth

## 🐛 حل المشاكل

### مشاكل شائعة
1. **لا تظهر البيانات**: تحقق من الاتصال بالإنترنت وإعدادات Supabase
2. **الحفظ لا يعمل**: تأكد من تعبئة جميع الحقول الإلزامية
3. **البحث لا يجد**: تحقق من إعدادات الحقول القابلة للبحث

### تصحيح الأخطاء
- افتح أدوات المطور في المتصفح (F12)
- تحقق من console للأخطاء
- راقب علامة التبويب Network للطلبات

## 📝 الترخيص

هذا المشروع مفتوح المصدر ويمكن استخدامه وتعديله بحرية.

## 🤞 المساهمة

للمساهمة في المشروع:
1. Fork المشروع
2. أنشئ فرعًا جديدًا
3. أضف التغييرات
4. أرسل Pull Request

## 📞 الدعم

لأي استفسارات أو مشاكل:
- افتح Issue في GitHub
- راجع الوثائق والتعليمات

---

**DOMS** - محرك طلبات ديناميكي 🚀
